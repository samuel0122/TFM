<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group
        android:id="@+id/group_list_edit_mode"
        android:visible="false">
        <item
            android:id="@+id/action_select_all"
            android:checkable="true"
            android:icon="@drawable/ic_checkbox_unchecked"
            android:title="Toggle select all"
            app:showAsAction="ifRoom" />
        <item
            android:id="@+id/action_delete_pages"
            android:icon="@drawable/ic_confirm_delete"
            android:title="Delete selected pages"
            app:showAsAction="ifRoom" />
        <item
            android:id="@+id/action_disable_edit_mode"
            android:icon="@drawable/ic_close"
            android:title="Cancel selection"
            app:showAsAction="ifRoom" />
    </group>

    <group
        android:id="@+id/group_pages_list"
        android:visible="true">
        <item
            android:id="@+id/action_edit_book"
            android:icon="@drawable/ic_edit"
            android:title="Edit book"
            app:showAsAction="ifRoom" />
        <item
            android:id="@+id/action_delete_book"
            android:icon="@drawable/ic_delete"
            android:title="Delete book"
            app:showAsAction="ifRoom" />
        <item
            android:id="@+id/action_enable_edit_mode"
            android:icon="@drawable/ic_list_edit_mode"
            android:title="Edit pages list"
            app:showAsAction="ifRoom" />
    </group>
</menu>